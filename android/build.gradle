buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 24
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
        youtubedlAndroidVersion = "0.18.1"
    }
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }

    // Workaround for legacy libraries that still call jcenter()
    // Gradle 9.0 removed the jcenter() method from RepositoryHandler
    repositories.all { repo ->
        if (repo.hasProperty('url') && repo.url.toString().contains("jcenter")) {
            repo.url = URI.create("https://repo1.maven.org/maven2/")
        }
    }
}

// Workaround for Gradle 9.0 task dependency issues with React Native libraries
subprojects { subproject ->
    afterEvaluate {
        // Fix task dependency ordering for codegen artifacts
        tasks.matching { it.name.endsWith("SourcesJar") }.configureEach { sourcesJarTask ->
            def codegenTask = tasks.findByName("generateCodegenArtifactsFromSchema")
            if (codegenTask != null) {
                sourcesJarTask.dependsOn(codegenTask)
            }
        }
    }
}

apply plugin: "com.facebook.react.rootproject"

